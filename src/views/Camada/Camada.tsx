import React from "react";
import { View, Text } from "react-native";

const Camada = () => {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center", backgroundColor: 'black' }}>
      <Text style={{ fontSize: 20 }}>Camada!</Text>
    </View>
  );
};

export default Camada;